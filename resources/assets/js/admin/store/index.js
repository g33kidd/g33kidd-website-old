import Vue from 'vue'
import Vuex from 'vuex'

// Import api and store modules
//

Vue.use(Vuex)

const state = {}
const mutations = {}
const actions = {}
const getters = {}

const store = new Vuex.Store({
    state,
    mutations,
    actions,
    getters,
    modules: {
        
    }
})

export default store
